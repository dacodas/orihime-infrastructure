apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test-ingress
spec:
  rules:
  - host: container-registry.dacodastrack.com
    http:
      paths:
      - path: 
        backend:
          serviceName: registry
          servicePort: 80
