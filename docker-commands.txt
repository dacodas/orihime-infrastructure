Just so that I don't lose these...

docker run --name goo-local-container \
       --network sbcl-network \
       --hostname goo-local-container \
       --network-alias goo-local-container \
       -d goo-local       

docker run --name mysql-container \
       --network sbcl-network \
       --hostname mysql-container \
       --network-alias mysql-container \
       --volume orihime-mysql-volume:/var/lib/mysql-volume/ \
       -e MYSQL_ROOT_PASSWORD=dacodastrackiscool \
       -d dacoda-mysql-server

docker run --name sbcl-container \
       --network sbcl-network \
       --hostname sbcl-container \
       --publish 4005:4005 \
       --volume goo-dictionary-volume:/var/lib/goo/data/ \
       --network-alias sbcl-container \
       -d sbcl-testing 

docker exec -i -t mysql-container /bin/bash
